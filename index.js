/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';
import {name as MasGlobalTestApp} from './app.json';

AppRegistry.registerComponent(MasGlobalTestApp, () => App);
